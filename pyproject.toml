[project]
name = "llms-from-scratch"
version = "0.1.0"
description = "Interactive textbook for building LLMs from scratch"
requires-python = ">=3.10"
dependencies = [
    "numpy>=1.24.0",
    "matplotlib>=3.7.0",
    "torch>=2.0.0",
    "jupyter>=1.0.0",
    "ipykernel>=7.2.0",
]
